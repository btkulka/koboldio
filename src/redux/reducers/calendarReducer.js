
const initialState = {
    recurringEvents: [],
    singleEvents: []
}

export default function(state = initialState, action){
    if (action.type === null){

    } else {
        return state;
    }
}